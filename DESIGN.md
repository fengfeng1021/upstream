# 上游 · upstream ／ 設計系統

這份文件先於任何一行介面程式碼。每個物件的外觀、狀態、動效與互動邏輯都在這裡定好，
實作只是把它寫成 CSS 與 JS。收工時回頭逐條驗收。

---

## 0 ／ Design Read

> Reading this as: an **instrument-panel site** for Taiwanese retail investors asking whether
> Taiwan equities currently have external support, with a **hydrological gauging-station**
> language, leaning toward native CSS + system mono + GSAP flow mechanics, dual-theme,
> zero decoration.

三個旋鈕：

| 旋鈕 | 值 | 為什麼不是預設值 |
|---|---|---|
| `DESIGN_VARIANCE` | **6** | 基準是 8。這是受監管題材，版面誇張會削弱可信度；但它仍是要讓人願意看的東西，不能掉到公部門的 3。 |
| `MOTION_INTENSITY` | **5** | 基準是 6。全站動效只有一種語意來源：**水往下游流**。§5 每一條動效底下那句「使用者學到什麼」是它存在的唯一理由。 |
| `VISUAL_DENSITY` | **5** | 基準是 4。這裡真的有數字要讀；但主角是單一讀數而非儀表牆——那正是我們批評對手的地方（48 張卡片 vs 一句話）。 |

---

## 1 ／ 世界：水文測站

### 1.1 為什麼是這個世界

產品論點是「大家都以為台股在下游」。錢被想像成水一樣流動：上游有水庫（美元流動性），
中間有閘門（台幣匯率），有流量計（外資買賣超），下游有水位（加權指數）。

這個世界之所以對，正是因為它同時能演出**那個想像**與**資料的實況**：
同一組水位計，可以依序漲，也可以一起漲。而實測告訴我們是後者。
世界要能承載反證，否則它只是包裝。

水文量測有一整套現成、精確、而且**不需要解釋**的視覺語言：水位標尺、高水位痕跡、
基準點銘牌、流向箭頭。台灣人對這套語言有直覺——圳路、水利會、颱風天的水位警戒。
把抽象的統計量換成水位，使用者不必先學統計就看得懂，這正是「一眼看懂」的做法。

而且它自帶誠實：**水位計不預測明天會不會下雨。** 它只告訴你現在幾公尺、
以及牆上那些痕跡代表以前到過幾公尺。這正好是 PRODUCT.md 六條硬規則要求的形狀。

### 1.2 和前兩個站的區隔（強制）

逐項對照，任何一項都不共用：

| | visible-money-tw | 刻度 · vernier | 上游 · upstream |
|---|---|---|---|
| 物質 | 紙。複寫單據、騎縫虛線、硃砂印 | 金屬。陽極鋁面板、蝕刻髮絲刻線 | 混凝土。模板澆置面、油漆刻度、水痕 |
| 底色 | 暖（`#F2F2EE` 紙白） | 冷（`#EBEDEF` 鋁灰） | 中性偏橄欖（`#E9E8E3` 混凝土） |
| 形狀鎖 | 圓角 0 | 圓角 3px（銑削倒角） | 圓角 6px（模板倒角），全站唯一值 |
| 強調色 | 印泥靛藍 `#123A72` | 青藍 `#0B6E8C` | 測水橙 `#8F4E04`／`#F0A24B` |
| 主結構線 | 表格框線與底線填寫格 | 雙軌分度尺 | 垂直水位標尺與水平高水位痕 |
| 主要動作 | 蓋章（落下、定住） | 滑移（沿軌道走、停在刻度） | 流下（由上游往下游傳，帶延遲） |
| 使用者做的事 | 填數字 → 得結果 | 說出持有 → 看見在分布哪一格 | 看一條鏈 → 知道哪一環是通的、哪一環是斷的 |

### 1.3 招牌元素：水位標尺與高水位痕

全站的視覺簽名，在不同尺度上重複同一個東西：

```
   ┌───┐  ← 頂端＝歷史最高
   │   │
   │───│  ← 高水位痕（每一道 = 一個歷史觀測）
   │───│
   │▓▓▓│  ← 現在的水位（accent 實心橫帶）
   │───│
   │───│
   │───│  ← 痕跡愈密＝分布愈集中在這一段
   └───┘  ← 底端＝歷史最低
        第 87 百分位 · 近 20 年 240 個月
```

- **痕跡不是裝飾格線，是資料。** 每一道橫線是一個真實的歷史觀測。
  刪掉任何一道都會讓分布的形狀改變，所以它不是裝飾（taste §9.F 禁裝飾格線）。
- **痕跡的疏密就是分布密度。** 使用者不必看直方圖，密的地方就是常出現的水位。
- **百分位一定跟視窗寫在一起。** 實測顯示同一個值換視窗會反轉訊息
  （10 年減 3 個月利差在 5 年視窗第 80、20 年視窗第 37 百分位）。
  所以 `.u-gauge` 沒有拿到 `window` 參數時**直接不渲染**，這是元件契約，不是提醒。

---

## 2 ／ 鎖

三個鎖在收工前逐項機械檢查，任一條不過就不算完成。

**主題鎖。** 全站 `color-scheme: light dark`，跟隨系統。沒有任何一個區塊反轉。
切換主題時語意不變：漲永遠是紅、跌永遠是綠、水永遠是橙。

**色彩鎖。** 強調色只有一個：測水橙。它只有一個語意：**「水」，也就是正在流動或正在被指的那個量**。

| 類別 | 位置 |
|---|---|
| 現在的水位 | `.u-gauge__now`、`.u-chain__node[data-active]` |
| 流動路徑 | `.u-chain__link`（實線時）、`.u-flow__head` |
| 焦點環 | `:focus-visible`、`.skip-link` |
| 選取 | `.u-dial__opt[aria-pressed="true"]`、`.u-masthead__link[aria-current]` |
| 主按鈕 | `.u-btn`（`.u-btn--ghost` 不填色） |
| 連結 | `a:hover` 的底線色 |

不用在裝飾、不用在圖示。紅與綠是**語意色**（台灣制：漲紅跌綠），只出現在數字與正負號上，
永遠不用於品牌或裝飾。橙與紅的色相差 36°（light）／34°（dark），且**兩者永不出現在同一個形狀上**
（橙在標尺與連線，紅綠只在數字），不會誤讀。

**形狀鎖。** 圓角一律 6px。按鈕、輸入框、面板、標尺、色塊圖例全部 6px。
沒有膠囊、沒有圓形、沒有 12px 軟角。6px 是模板澆置留下的倒角，
它讓邊看起來是被灌出來的而不是被畫出來的。

---

## 3 ／ 代幣

寫進 `assets/css/tokens.css`。所有對比度已用 WCAG 相對亮度算過，標註在旁。

### 3.1 顏色

```css
:root {
  color-scheme: light dark;

  /* 面（乾混凝土） */
  --face:      #E9E8E3;
  --face-2:    #F4F3EF;  /* 抬起的面板 */
  --face-sunk: #DBDAD4;  /* 標尺槽、輸入槽 */

  /* 字 */
  --ink:       #181A17;  /* 對 face 14.28:1 */
  --ink-2:     #3F423C;  /* 對 face 8.33:1 */
  --ink-3:     #5A5E56;  /* 次要說明。對 face 5.40:1、face-2 5.97:1、sunk 4.73:1 */

  --rule:        #C6C5BE;  /* 主刻線 */
  --rule-strong: #9B9A92;  /* 每五格的長刻線 */
  --rule-faint:  #DEDDD7;  /* 高水位痕 */

  /* 強調（水）。對 face 5.25:1、face-2 5.80:1、sunk 4.60:1；白字壓在上面 6.44:1 */
  --accent:     #8F4E04;
  --accent-hi:  #A75B06;
  --accent-ink: #FFFFFF;
  --accent-wash:#F0E4D5;

  /* 語意（台灣制：漲紅跌綠）。對 face：up 5.55:1、down 5.38:1 */
  --up:        #B0202A;
  --down:      #0A6B3D;
  --warn:      #8A5300;
  --ghost:     #B4B3AB;  /* 對照組、未選取 */
}

@media (prefers-color-scheme: dark) {
  :root {
    --face:      #1A1B18;  /* 濕混凝土 */
    --face-2:    #232420;
    --face-sunk: #111210;

    --ink:       #EFEFEA;  /* 對 face 15.00:1 */
    --ink-2:     #B7B9B1;  /* 對 face 8.72:1 */
    --ink-3:     #8D9088;  /* 對 face 5.34:1、face-2 4.82:1 */

    --rule:        #33352F;
    --rule-strong: #4F524A;
    --rule-faint:  #262823;

    /* 對 face 8.22:1、face-2 7.42:1。深字壓在上面 8.40:1 */
    --accent:     #F0A24B;
    --accent-hi:  #FFBC74;
    --accent-ink: #231604;
    --accent-wash:#3A2A15;

    --up:        #E85C60;  /* 對 face-2 4.56:1 */
    --down:      #34B87A;  /* 對 face-2 6.16:1 */
    --warn:      #D9A23A;
    --ghost:     #5C5F57;
  }
}
```

### 3.2 字體

```css
--font-cjk:  "Noto Sans TC", -apple-system, BlinkMacSystemFont, "PingFang TC",
             "Microsoft JhengHei", "Segoe UI", sans-serif;
--font-mono: "JetBrains Mono", "Roboto Mono", ui-monospace, "SF Mono",
             "Cascadia Mono", Consolas, monospace;
```

**不從 Google Fonts CDN 載入，字體堆疊一路退到系統字。** 這一站對外的說法是
「零金流、不追蹤、離線可用」，而一個 `<link>` 到 fonts.googleapis.com 會在每次載入時
把使用者的 IP 與 referrer 交出去，那句話就不成立了。
`tools/audit.mjs` 的「沒有從第三方網域載入任何資源」把這件事鎖成硬性檢查。

代價是字形個性可能退掉。可以接受，因為數字對齊靠的是 `font-variant-numeric: tabular-nums`，
任何一款等寬字都給得出來，掉的不是可讀性。

**數字一律 mono、一律 `tabular-nums`。** 同一欄的數字必須對齊到同一條格線，
否則眼睛要重新對焦才能比較大小，那就不是儀器了。

```css
--t-xs:   0.75rem;    /* 12  刻度標籤 */
--t-sm:   0.8125rem;  /* 13  註記、抓取日 */
--t-base: 0.9375rem;  /* 15  內文 */
--t-md:   1.0625rem;  /* 17  判讀句 */
--t-lg:   1.3125rem;  /* 21  小標 */
--t-xl:   1.625rem;   /* 26  區塊標題 */
--t-2xl:  2.0625rem;  /* 33 */
--t-3xl:  2.625rem;   /* 42  首屏標題（桌機） */
--t-readout: clamp(2.5rem, 7vw, 4.25rem);  /* 主讀數，唯一的流體字級 */
```

### 3.3 尺寸

```css
--s-1: 4px;  --s-2: 8px;   --s-3: 12px;  --s-4: 16px;
--s-5: 24px; --s-6: 32px;  --s-7: 48px;  --s-8: 64px;  --s-9: 96px;

--radius: 6px;   /* 形狀鎖，全站唯一值 */
--hair: 1px;     /* 痕跡寬 */
--gauge-w: 28px; /* 標尺寬（md） */
--tap: 44px;     /* 觸控目標最小邊 */
--wrap: 1180px;  /* 內容最大寬 */
--read: 62ch;    /* 內文最大寬 */
```

### 3.4 動效常數

```css
--e-flow: cubic-bezier(0.33, 0, 0.15, 1);   /* 水流：慢起、快中段、緩停 */
--e-rise: cubic-bezier(0.22, 1, 0.36, 1);   /* 水位上升：快出、慢停 */
--d-fast: 0.18s;  --d-base: 0.34s;  --d-slow: 0.6s;
```

GSAP 對應：`--e-rise` = `power4.out`，`--e-flow` = `power2.inOut`。

---

## 4 ／ 物件

每個物件寫清楚：長什麼樣、有哪些狀態、怎麼互動、動效是什麼。
實作時一個物件一個 CSS 區塊，命名用 `.u-` 前綴。

### 4.1 `.u-masthead` 站頭

站名 + 資料抓取日 + 四個工具連結。桌機 **64px 高、單行**，手機 56px 並把連結收進橫向捲動列。

- 底部一條 `--rule` 髮絲線。
- 站名 CJK 900 字重 `--t-lg`，後接 12px 垂直刻線，再接小字 `upstream`（mono `--t-xs`, `--ink-3`）。
- 抓取日永遠在右上：`資料 2026-08-04`（mono `--t-xs`）。硬規則 4 的視覺承諾，每頁都要有。
- 目前所在的工具：底部 2px `--accent` 底線。不用色塊、不用膠囊。

禁止：logo 圖形、hamburger 以外的圖示、任何 CTA 按鈕。站頭不賣東西。

### 4.2 `.u-gauge` 水位標尺

招牌元素。**沒有 `window` 參數就不渲染**（見 §1.3）。

```html
<div class="u-gauge" data-window="240" role="img"
     aria-label="美元廣義指數 119.70，位於近 20 年 240 個月的第 82 百分位">
  <div class="u-gauge__well">
    <div class="u-gauge__marks"></div>   <!-- 高水位痕，每道一個歷史觀測 -->
    <div class="u-gauge__now"></div>     <!-- 現在的水位 -->
  </div>
  <div class="u-gauge__label">…</div>
</div>
```

- 痕跡用 `<canvas>` 或單一 `repeating-linear-gradient` 疊多層畫，**不生 DOM 節點**
  （240 個觀測不能生 240 個 div）。痕跡色 `--rule-faint`，透明度隨重疊自然加深。
- 現在的水位：3px `--accent` 實心橫帶，左右各突出 3px，左端帶一個 5px 實心三角
  （CSS `clip-path`，不是 SVG 圖示）。
- **標籤兩行**，第二行不可省略：
  第一行是值與單位（mono `--t-lg`）；第二行是 `第 82 百分位 · 近 20 年 240 個月`
  （mono `--t-xs`, `--ink-3`）。儀器不報告它讀不出來的精度。
- 三種尺寸：`sm` 高 64px（內嵌句子旁）／`md` 高 140px（預設）／`lg` 高 260px（首屏主件）。

**狀態：**

| 狀態 | 表現 |
|---|---|
| rest | 痕跡 `--rule-faint`，水位 `--accent` |
| hover／focus | 水位帶亮到 `--accent-hi`，標籤第二行加出 `最低 X · 最高 Y` |
| 樣本不足（n < 24） | 不畫痕跡，改一行 `--ink-3` 說明「樣本 N 個月，不足以給百分位」 |
| 視窗不一致 | 畫**兩條**水位帶（5 年一條、20 年一條），並列兩個百分位。不一致本身就是資訊 |

### 4.3 `.u-chain` 傳導鏈

工具 1 的主件，也是整個站的招牌畫面。

```
 ┌──────┐      ┌──────┐      ┌──────┐      ┌──────┐
 │ 美元 │ ───▶ │ 台幣 │ ╌╌╌▶ │ 外資 │ ───▶ │ 台股 │
 │ gauge│      │ gauge│      │ gauge│      │ gauge│
 └──────┘      └──────┘      └──────┘      └──────┘
   第 82        第 61         本月         第 94
              實線＝有量到關聯   虛線＝這一環的證據弱
```

- 每一節點是一個 `.u-gauge`（`sm`）加標題。
- **連線是這個元件的重點**：`.u-chain__link` 有兩種狀態，由**資料**決定，不由設計決定：
  - **實線**：這一環的歷史關聯通過樣本與強度門檻，線上標 `r = -0.62 · n = 240`。
  - **虛線 `--ghost`**：證據弱。線上標 `關聯弱 · r = -0.11`，並在下方一行說明為什麼。
- **虛線不是錯誤狀態，是這個站最重要的輸出。** 使用者真正的損失來自
  「以為某條鏈成立所以照著做」，所以斷環要跟通環一樣顯眼，用同樣的字級。
- 每條連線標出**落後期數**（`落後 1 個月`），因為傳導不是同時發生的。

### 4.4 `.u-readout` 讀數

一個大數字，加上單位，加上樣本。這是儀器的臉。

```
     +2.4 %
     ─────────────────
     美元廣義指數 近 3 個月變動
     樣本 240 個月 · 資料 2026-07
```

- 數字：mono `--t-readout`，500 字重，`tabular-nums`，`--ink`。
- 單位：`--t-lg`，`--ink-2`，緊貼右側，baseline 對齊，不上標。
- 下方髮絲線 `--rule`，寬度等於數字寬（`width: fit-content`）。
- 說明兩行，第二行是**樣本數與資料月份**，不可省略。
- 正負值：只有明確是報酬或漲跌的數字才套 `--up`／`--down`。純比例、月數、檔數維持 `--ink`。

### 4.5 `.u-windows` 多視窗比對

工具 2 的主件。三個視窗（3／6／12 個月）並排，**一致與不一致都要看得見**。

```
  黃金      3M ▁▁▁▁▏      6M ▁▁▁▏       12M ████▏
                 -12.3%        -14.1%         +23.0%
            ▲ 三個視窗不一致：短期在跌、長期在漲
```

- 每個視窗一條水平長條，共用同一條基線與同一把尺。
- **一致**（三個同號）：三條同色（`--up` 或 `--down`），下方一行「三個視窗一致」。
- **不一致**：各自著色，下方一行明說哪個視窗跟其他不同。
- 文案一律寫「歷史上多視窗一致的訊號較穩定，而且它降的是波動不是增報酬」，
  永遠不寫「該買」。**不提供視窗最佳化，不提供自訂視窗**——那會變成參數挖掘。

### 4.6 `.u-dial` 參數旋鈕

使用者自訂視窗與門檻的地方。硬規則 3：判斷標準的所有權屬於使用者。

- 形狀：一排刻度按鈕，選中的填 `--accent`、文字 `--accent-ink`。
  例如百分位視窗 `5 年 · 10 年 · 20 年 · 全部`。
- 每個旋鈕上方一行標籤，**標籤後面不加建議值、不加「建議」「一般而言」**。
- 若某個值因資料長度不足而不可選，該格 `aria-disabled` 並在下方以 `--t-xs` 說明
  「這條序列只有 N 個月」。這是資料事實陳述，不是建議。

### 4.7 `.u-verdict` 判讀

工具的輸出。全站法規風險最高的文字，所以它是**模板**，不是自由發揮。

結構固定三段：

```
【現況】美元廣義指數在近 20 年的第 82 百分位，台幣兌美元 32.27，近 3 個月貶值 2.1%。
【鏈結】美元 → 台幣這一環量得到（r = -0.62，n = 240）；台幣 → 外資這一環量不到（r = -0.11）。
【邊界】以上是 2006-01 到 2026-07 的歷史關聯，不預測未來，不構成任何建議。
```

- 三段各一個 `.u-verdict__line`，`--t-md`，數字用 mono 並套語意色。
- 【邊界】那一行永遠存在，`--t-sm`, `--ink-3`，不可被使用者關閉。
- **允許的動詞**：是、為、落在、佔、來自、同時、相差、涵蓋、顯示、量得到、量不到。
- **禁止的動詞**：建議、應該、適合、值得、可以考慮、比較好、優於、推薦、避免、將會、預期。
- **禁止的名詞**：目標價、支撐、壓力、停損、停利、買點、賣點、進場、出場。

### 4.8 `.u-datestamp` 抓取日戳

每一個承載資料的區塊右上角。mono `--t-xs`，`--ink-3`，前面一條 8px 髮絲線。
格式 `2026-08-04`。這是硬規則 4 的視覺承諾，也是相對於即時報價站的差異點，所以它不躲起來。

**額外規則：序列自己的最後觀測月份與抓取日不同時，兩個都印。**
CPI 落後兩個月是常態，把落後的數字畫在最新那一格是在假裝我們知道現在的通膨。

### 4.9 狀態組

**載入。** 骨架必須是最終版面的形狀：標尺畫出來但痕跡是 `--rule-faint` 且沒有水位帶，
讀數位置放一個等寬的 `--face-sunk` 色塊。不用轉圈圈。

**空。** 這個站沒有真正的空狀態（資料是固化的），唯一的空是使用者把篩選縮到沒有結果，
文案「這個組合沒有資料，把視窗放寬看看」。不用警告色。

**錯誤。** 只有一種真實錯誤：`fetch('assets/data/snapshot.json')` 失敗。
內嵌在版面內，`--warn` 左側 3px 直條，文案「資料檔沒有載入，重新整理或檢查網路」。
不用 toast，因為這不是暫時性的。

**資料不足。** 不是錯誤，是常態。用 `--ink-3` 一行說明取代該區塊，
講清楚「這條序列從 2006-01 開始，不足以給 20 年百分位」。這種誠實本身就是產品的一部分。

**過期。** build 的過期守門若放行但序列仍偏舊，介面上以 `--warn` 標註
「這條序列最後更新於 YYYY-MM」。已停止發布的序列一律不當現況顯示。

### 4.10 按鈕

只有兩種：

- `.u-btn`（主）：`--accent` 填色、`--accent-ink` 文字（淺色 6.44:1、深色 8.40:1）、
  radius 6px、高 40px、padding 0 20px、`--t-base` 500 字重。
- `.u-btn--ghost`：透明底、1px `--rule-strong` 邊、`--ink` 文字。

`:active` 時 `translateY(1px)`，`:focus-visible` 時 2px `--accent` 外環。
**標籤最多四個中文字**，桌機絕不換行。整站不出現兩個同義的行動呼籲。
CTA 文案全站統一為「往上游看」，三處用同一個字串。

---

## 5 ／ 動效語彙

`MOTION_INTENSITY: 5`。每一條動效下面那句「使用者學到什麼」是它存在的唯一理由，
說不出來就砍掉。全部只動 `transform` 與 `opacity`。

所有動效包在 `gsap.matchMedia()` 裡，`(prefers-reduced-motion: reduce)` 分支一律直接跳到終態。
GSAP 3.15 由 `assets/js/vendor/` 本機載入。

### 動詞 1 ／ 流下 `flow`

一個 `--accent` 的光點從左（上游）走到右（下游），每段之間停一下，四個節點依序亮起。
這是新聞與話術裡的版本，也是使用者腦中原本的模型。

- **學到什麼**：先把使用者既有的因果模型變成一個看得見的具體動作。
  沒有這一步，動詞 2 就沒有對照組。
- 用在：工具 1 的 `.u-chain`，模式切換在「你以為的」時。
- 實作：GSAP timeline，每段 `duration: 0.5`，節點 `onStart` 加 `data-active`。

### 動詞 2 ／ 同時亮 `simultaneous`

四個節點**在同一幀一起亮**，三條連線同時整條通電。零 stagger，零延遲。

- **學到什麼**：這才是資料的形狀。**這裡「沒有 stagger」本身就是全站最重要的一句話。**
  同期關聯 0.68／-0.45／-0.51 在四個子期間全部顯著，落後一期全部趨近於零，
  所以四環之間沒有可以搶先的空隙。使用者上一秒才看過依序版本，
  兩者的差異用一次切換就講完，不需要一段文字。
- **兩種模式共用完全相同的 DOM 與樣式，只有時間軸不同。**
  這是刻意的：差別必須只在時間，不能在別的地方，否則對照不成立。
- 實作：同一條 timeline 的第二種 mode，全部 `position: 0`，`power2.out`，0.3s。

### 動詞 2b ／ 斷流 `break`

某一環若在使用者選定的子期間裡關聯不顯著（|t| ≤ 2），連線切成虛線 `--ghost`，
光點走到該處**停下並淡出**。

- **學到什麼**：這一環在這段期間量不到。用「動作的缺席」講比用文字講快，
  而且使用者不會漏看——他正在追那個光點。
- 實測全期間四環的同期關聯都顯著，所以這個狀態主要出現在使用者把期間縮短、
  樣本變小的時候——那正是應該讓他看見「樣本不夠就量不到」的時刻。

### 動詞 3 ／ 水位上升 `rise`

標尺的水位帶從底部升到它的值，`power4.out`，0.34s。

- **學到什麼**：這個值是被量出來的，而且它有一個底（歷史最低）。
  從零升起讓使用者看見整段量程，而不只是最後停的位置。
- 用在：所有 `.u-gauge` 第一次進入視窗時。
- 實作：`scaleY` 0 → 1，`transform-origin: bottom`。`ScrollTrigger` `once: true`，`start: "top 80%"`。

### 動詞 4 ／ 留痕 `stain`

高水位痕由最舊到最新依序浮現，`stagger: { amount: 0.5 }`，只動 `opacity`。

- **學到什麼**：這個百分位是由 N 個真實觀測堆出來的，不是一個算出來的分數。
  順序本身在傳達「這裡有 240 個月」。
- 用在：`.u-gauge` 的 `md` 與 `lg` 尺寸（`sm` 不跑，太小看不出來）。
- 上限：觀測數超過 400 時 `amount` 不變、改分批，避免長尾。

### 動詞 5 ／ 對齊 `align`

工具 2 的三條視窗長條從各自的位置滑到共用基線上。

- **學到什麼**：三個視窗在比同一件事。滑到同一條基線這個動作本身
  就是「可以並排比較」的證明；一致或不一致在對齊那一刻才看得出來。
- 實作：`stagger: 0.08`，`power2.out`，0.4s。

### 全域規則

- 進場：`opacity` 0 → 1 加 `y` 12px，0.34s，`power4.out`，`ScrollTrigger.batch` 收集，
  `stagger: 0.06`。一個區塊只跑一次（`once: true`）。
- **禁**：`window.addEventListener('scroll')`、無限迴圈微動畫、視差、
  自訂游標、marquee（全站零個）、載入轉圈。
- 一頁最多一個 pin。工具 1 的傳導鏈是唯一的 pin，其他頁零 pin。

---

## 6 ／ 頁面與互動邏輯

### 6.0 檔案結構

```
index.html                  中樞
apps/chain/index.html       1 上游（傳導鏈）
apps/lead/index.html        2 誰在領先
apps/metal/index.html       3 貴金屬
apps/now/index.html         4 現在的環境
assets/css/  tokens.css base.css components.css hub.css app.css
assets/js/   upstream.js（共用：讀數、標尺、鏈、視窗比對、動效語彙）
             analytics.js（判讀層，與 tools/ 共用同一份算法）
             apps/chain.js lead.js metal.js now.js
             vendor/gsap.min.js ScrollTrigger.min.js CustomEase.min.js
assets/data/ snapshot.json（價格＋總經）taiwan.json（加權指數、外資、LBMA 金銀）
```

### 6.1 中樞 `index.html`

五個區塊，五個不同的版面族（taste §4.7 版面重複禁令）：

| 區塊 | 版面族 | 內容 |
|---|---|---|
| 首屏 | 左文右儀器（非置中） | 標題兩行、副標 18 字、一條**活的**傳導鏈跑「流下」動效、一個主 CTA |
| 四個工具 | 不對稱 bento，**恰好 4 格**（1 大 + 3 小） | 每格一句問題 + 一個從真實資料算出的縮小讀數 |
| 資料地基 | 滿版單軌時間尺 | 1990-01 → 2026-07 畫成一條軸，各序列的起始月標在上面 |
| 不做什麼 | 兩欄分組，稀疏分隔 | 六條硬規則收斂成三組兩條，組間才有髮絲線 |
| 頁尾 | 三欄 | 資料來源、抓取日、授權、原始碼連結 |

**首屏預算（taste 硬規則）**：文字元素最多 4 個。這裡用 3 個：標題、副標、CTA。
沒有 eyebrow、沒有版本標籤、沒有信任微條、沒有捲動提示、沒有地點時間條。
**全站 eyebrow 總數：0。**

### 6.2 工具 1 ／ 上游

**交互邏輯**

1. 進頁面：傳導鏈直接顯示**當前**四環水位，不需要任何輸入。
   摩擦最低，因為使用者來這裡就是要一眼看到現況。
2. 預設先跑一次「依序流下」（動詞 1），因為那是他腦中的版本。
3. 主件正下方是**唯一的主要控制項**：一個兩段切換
   `你以為的 ／ 實際量到的`。切到後者，同一條鏈改用「同時亮」（動詞 2）。
   切換是這一頁的產品本體，所以它是 `.u-btn` 尺寸，不是小開關。
4. 切換的同時，下方的關聯矩陣把對應的欄位highlight：
   「你以為的」highlight 落後 1 到 3 期那三欄（全部趨近於零），
   「實際量到的」highlight 落後 0 那一欄（全部顯著）。
   **數字與動效指向同一件事，兩邊必須同時動，否則使用者會以為那是兩個獨立的東西。**
5. `.u-dial` 讓使用者選估計期間（四個子期間或全部）與百分位視窗。
   **沒有預設的「正確」視窗**，第一次進入停在「全部」並明說
   「這是資料允許的最長期間，不是建議」。
6. 點任一環 → 展開該環細節：兩條序列疊圖、同期散布圖、逐年的滾動關聯。

**輸出**（`.u-verdict` 三段模板，見 §4.7）

**視覺主件**：`.u-chain`，四個 `.u-gauge` 加三條連線。

### 6.3 工具 2 ／ 誰在領先

**交互邏輯**

1. 進頁面：股／債／貴金屬／商品／匯五類的 3／6／12 個月變動並排。
2. `.u-dial` 選要不要換算成台幣計價。**這是台灣本位的關鍵開關**：
   一個台灣投資人持有美股，他真正承受的是美股報酬乘上匯率變動。
   預設**開啟**台幣計價，並明說「這是台灣投資人實際承受的口徑」。
3. 點任一類 → 展開該類底下各標的的同一組讀數。
4. 全站不提供排序切換（排序等於排名，踩硬規則 2）。順序固定為資料檔的 `classOrder`。

**輸出**：一致與不一致的計數，加上「多視窗一致歷史上降的是波動不是報酬」那句邊界。

### 6.4 工具 3 ／ 貴金屬

**交互邏輯**

1. 進頁面：LBMA 黃金 1968 起的長期水位標尺（`lg`），旁邊是白銀。
2. 主件是**敏感度**而不是估值：一張散布圖，橫軸是 DFII10 的月變動，
   縱軸是黃金的月變動，標出迴歸線與 beta，並附各子期間的 beta 表**含 R² 崩掉的那幾段**。
3. `.u-dial` 選子期間。使用者可以自己看到 2005–2021 的水位關係與 2024–2026 的水位關係差多少。
4. 頁面明確寫出那個異常：實質利率在近 20 年第 98 百分位，而黃金在歷史高點。
   **不解釋它，只呈現它。** 解釋就是預測。

### 6.5 工具 4 ／ 現在的環境

**交互邏輯**

1. 四個 `.u-gauge`（VIX、信用利差、金融情勢、殖利率曲線），全部 `md`。
2. `.u-dial` 選百分位視窗，**五年與二十年不一致時兩個都畫**。
3. 殖利率曲線下方是完整的歷史倒掛事件表，**含 2022-11 到 2024-11 那次沒有衰退的**。
   失敗案例跟成功案例同樣字級、同一張表，不註腳化。
4. VIX 那一格附一行實測事實：「歷史上 VIX 最高的五分之一，後續三個月報酬是最好的」，
   並且**不塗紅**。

---

## 7 ／ 影像策略

這個站不放照片。理由不是懶：

- 金融題材的圖庫照片（硬幣、上升箭頭、看盤的人）是這個品類最強的 slop 訊號，
  放了會直接摧毀可信度。
- taste §4.8 允許的第三條路是「真實的元件預覽」：**首屏的傳導鏈是活的**，
  它用 `snapshot.json` 與 `taiwan.json` 的真實資料即時算出四環狀態並跑「流下」動效。
  那不是用 div 拼的假截圖，它就是產品本身，只是縮小放在首屏。

視覺重量全部由**資料本身的形狀**承擔：水位標尺、高水位痕、傳導鏈、散布圖。

`favicon.svg`：一個極簡幾何標記，三道水位痕加一條水位線。
符合 taste 對手繪 SVG 的例外（單一、極簡的幾何標記）。

---

## 8 ／ 響應式

斷點 `sm 640 / md 768 / lg 1024 / xl 1280`。
`DESIGN_VARIANCE: 6` 的不對稱版面在 `< 768px` 一律塌成單欄（`width: 100%`, `padding: 0 16px`）。

| 物件 | `< 768px` |
|---|---|
| `.u-masthead` | 56px，工具連結變橫向捲動列，`scroll-snap-type: x mandatory` |
| `.u-chain` | **從水平四節點轉成垂直四節點**，連線變成垂直的，「流下」動效方向從左右改成上下。這個轉向是免費的，因為水本來就往下流 |
| `.u-gauge` | `lg` 降為 `md`；標籤兩行改為堆疊 |
| `.u-windows` | 三條長條維持水平（它們必須共用基線才能比較），改成縮短標籤 |
| `.u-readout` | `clamp()` 自動降到 2.5rem |
| 散布圖 | 維持正方形，寬度吃滿，點大小從 3px 降到 2px |

`min-height: 100dvh` 不用 `100vh`。首屏在 375×667 必須完整可見（標題、副標、CTA 全部在第一屏）。

---

## 9 ／ 收工驗收

實作完成後逐條跑，任一條不過就不算完成。

### 機械檢查（`node tools/audit.mjs`）

- [ ] 全站渲染文字中 `—` 與 `–` 出現次數為 **0**（含 alt、aria-label、按鈕文案）。中文破折號「——」一併禁用。
- [ ] eyebrow 數量為 **0**。
- [ ] `border-radius` 的相異值只有 `6px` 與 `0`（0 只用於滿版分隔線）。
- [ ] 強調色的每一個出現位置都在 §2 色彩鎖那張表裡，一個都不多。
- [ ] `window.addEventListener('scroll')` 出現次數為 **0**。
- [ ] marquee 與無限迴圈動畫數量為 **0**（載入骨架是唯一例外，且已被 reduced-motion 關掉）。
- [ ] §4.7 的禁用動詞與禁用名詞在全站 HTML 與 JS 字串中零命中
      （唯一例外是【邊界】固定句尾的「不構成任何建議」，grep 時先排除）。
- [ ] 每個承載資料的區塊都有 `.u-datestamp`。
- [ ] **每一個 `.u-gauge` 都帶 `data-window`**，沒有帶的直接算失敗（§1.3 元件契約）。
- [ ] 沒有從第三方網域載入任何資源。外連只有一個：原始碼倉庫。

### 視覺檢查（CDP headless）

Windows 視窗有 ~500px 最小寬度，`--window-size=390` 會給出被裁切的版面，
所以一律走 headless CDP `Emulation.setDeviceMetricsOverride`，不看視窗截圖。

- [ ] 375 / 390 / 768 / 1280 / 1440 五個寬度各一輪，淺色與深色各一輪，五頁零橫向溢出。
- [ ] 首屏在 375×667 完整可見，CTA 不需捲動。
- [ ] 站頭桌機單行且 ≤ 64px。主按鈕標籤桌機不換行。
- [ ] 文字對比：淺色與深色 × 桌機與手機 × 五頁，**0 條不合格**。

### 互動檢查

- [ ] 每個 `.u-dial` 用鍵盤走得完（真按鍵，走 CDP `Input.dispatchKeyEvent`；
      頁面裡 dispatch 的 `KeyboardEvent` 是 untrusted 的，驗不到東西）。
- [ ] `prefers-reduced-motion: reduce` 下全部動效跳終態，`document.getAnimations()` 裡 0 個 running。
- [ ] `snapshot.json` 改名模擬失敗 → 五頁都顯示 §4.9 的錯誤區塊，不是白畫面、畫面上沒有 NaN。
- [ ] 斷環的那一環：光點確實停住、連線確實是虛線、說明確實出現。

### 法規檢查（PRODUCT.md 六條）

- [ ] 沒有任何標的排名、篩選結果、預設勾選、排序切換。
- [ ] 沒有任何門檻是設計者預設的；體制門檻由資料自己的分位算出，並在介面上寫出它是怎麼來的。
- [ ] 沒有價位、支撐壓力、停損停利、買賣轉折、目標價的任何形式。
- [ ] 每一頁都有抓取日；沒有任何即時或盤中資料。
- [ ] 沒有 K 線、均線、動能指標的圖形。
- [ ] 沒有廣告、聯盟連結、開戶連結、email 收集、會員；沒有任何第三方請求。
